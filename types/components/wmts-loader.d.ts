export default interface wmtsLayerConfiguration {
    capability: string;
    layer: string;
    projection: string;
    name: string;
    thumbnail: string;
}
export default class WMTSLoader {
    constructor();
}
